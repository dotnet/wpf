    <!--=================================================================
    Licensed to the .NET Foundation under one or more agreements.
    The .NET Foundation licenses this file to you under the MIT license.
    See the LICENSE file in the project root for more information.
    ==================================================================-->

    <!-- [[Aero.NormalColor, Aero2.NormalColor, Luna.Homestead, Luna.Metallic, Luna.NormalColor, Royale.NormalColor, Classic]] -->

    <!--=================================================================
            TimePicker
        ==================================================================-->

    <Style x:Key="DefautltTimePickerStyle" TargetType="{x:Type TimePicker}">
        <Setter Property="Background" Value="Green" />
        <Setter Property="Foreground" Value="Red" />
        <Setter Property="BorderBrush" Value="Black" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TimePicker}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border x:Name="ContentBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <StackPanel Orientation="Horizontal">
                                <ComboBox x:Name="HoursComboBox"
                                          Width="50"/>
                                <TextBlock Text=":" VerticalAlignment="Center"/>
                                <ComboBox x:Name="MinutesComboBox"
                                          Width="50"/>
                                <ComboBox x:Name="AmPmComboBox"
                                          Width="50"/>
                            </StackPanel>
                        </Border>
                        <Button x:Name="PART_Button"
                                Grid.Column="1"
                                Width="24"
                                Height="24"
                                Margin="0,5,4,0"
                                Padding="0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Top"
                                Background="Transparent"
                                BorderBrush="Transparent">
                            <TextBlock Background="{TemplateBinding Background}" Text="&#xE787;" FontFamily="Segoe Fluent Icons" FontSize="14"/>
                        </Button>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="Blue"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="Gray"/>
                            <Setter TargetName="ContentBorder" Property="Background" Value="LightGray"/>
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="Gray"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefautltTimePickerStyle}" TargetType="{x:Type TimePicker}" />
